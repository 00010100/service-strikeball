const authValidate = require('./auth')
const userValidate = require('./user')
const teamValidate = require('./team')
const requestValidate = require('./request')

module.exports = {
  authValidate,
  userValidate,
  teamValidate,
  requestValidate
}
