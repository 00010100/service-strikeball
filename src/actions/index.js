const authAction = require('./auth')
const userAction = require('./user')
const teamAction = require('./team')
const requestAction = require('./request')

module.exports = {
  authAction,
  userAction,
  teamAction,
  requestAction
}
